import { ArgsTable, Preview, Story, Meta } from '@storybook/addon-docs';
import { Canvas, Example } from '../';

<Meta title='Components/MDX' parameters={{ previewTabs: { canvas: { hidden: true } } }} />

# MDX story example

```tsx live expanded
render(() => {
    const [counter, setCounter] = React.useState(0);
    return (
        <>
            <h2>Super live counter: {counter}</h2>
            <button type='button' onClick={() => setCounter((c) => c + 1)}>
                Increment
            </button>
        </>
    );
});
```

<Example live={true} code={`<h4>Wow, so directly</h4>`} />

<Canvas live={true}>
    <h1>Wow, custom Canvas</h1>
</Canvas>
